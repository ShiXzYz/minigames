#pragma once

extern const float win_height;
extern const float win_width;

extern const float paddle_width;
extern const float paddle_height;
extern const float paddle_speed;

extern const float ball_size;
extern const float ball_start_speed;

extern const float max_bounce_speed;
extern const float spin_factor;

// Selectable win scores in the mode-select screen. -1 means unlimited (no win score).
extern const int win_score_options[];
extern const int win_score_options_count;
